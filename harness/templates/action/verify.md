## Read-Back Verification

After writing any file or artifact, read it back and confirm the content, the
path, and the format the task specified: filename, extension, field names,
units. Verify each artifact as you produce it, not all of them at the end. A
successful write call is not evidence of delivery.
