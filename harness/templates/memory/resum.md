## Rolling Summary

Keep a `summary.md` of confirmed facts only. When several new facts land,
rewrite it as one compact block of the current state and drop the superseded
detail. Afterwards reason from the summary, not the raw output.
