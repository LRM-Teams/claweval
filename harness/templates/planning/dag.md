## Dependency Planning

Write down the subtasks and, for each, what must be known before it can start.
Do the ones with no unmet dependency first, and batch independent calls together
rather than interleaving them. Never start a dependent subtask on a guessed
input.
